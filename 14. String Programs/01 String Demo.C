#include<stdio.h>
#include<conio.h>
#include<string.h>

int main()
{
            char City[20] = "Kolhapur City";

            strrev(City);

            printf("\n String = %s.\n", City);

                                                                                     // printf("\n Given String is => %s", cArr);

            _getch();
            return 0;
}
